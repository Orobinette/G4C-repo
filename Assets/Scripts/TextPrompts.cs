using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TextPrompts : MonoBehaviour
{
    /*
    [SerializeField] MeshRenderer pickupRender;

     void OnCollisionEnter2D(Collision2D other) 
    {
        if (other.gameObject.tag == "bin")
        {
            PopUp(true);
        }
    }

    void OnCollisionExit2D(Collision2D other) 
    {
        if (other.gameObject.tag == "bin")
        {
            PopUp(false);
        }
    }

    void PopUp(bool touchingBin) 
    {
        if(touchingBin == true)
        {
            pickupRender.enabled = true;
        }
        else 
        {
            pickupRender.enabled = false;
        }
    }
    */
}
